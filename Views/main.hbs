<!-- To put files in each other -->

<div class="cover">
    {{#if islogin}}
        {{#if isOwner}}
            {{> ownernav}}
            {{> ownermain}}
        {{else}}
            {{> usernav}}
            {{> usermain}}
        {{/if}}
    {{else}}
        {{> guestnav}}
        {{> guestmain}}
    {{/if}}
