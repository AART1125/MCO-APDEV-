<!-- To put files in each other -->

<div class="cover">
    {{#if islogin}}
        {{#if isOwner}}
            {{> ownernav}}
        {{else}}
            {{> usernav}}
        {{/if}}
        {{>usermain}}
    {{else}}
        {{> guestnav}}
        {{> guestmain}}
    {{/if}}